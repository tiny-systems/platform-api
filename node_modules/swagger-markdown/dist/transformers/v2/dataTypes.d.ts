import { SchemaInterface } from '../../models/Schema';
/**
 * Transform data types into common names
 * @param {Schema} schema
 * @return {String}
 */
export declare const dataTypeResolver: (schema: SchemaInterface) => string;
